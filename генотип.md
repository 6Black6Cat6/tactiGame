## Генетика магических рас (переработанная версия)

### 1. Общая модель

Все магические способности у гуманоидных рас определяются **локусом M** (мана-транспортёр), кодирующим трансмембранный белок, способный утилизировать свободную магическую энергию.

У разных рас существуют **аллельные варианты** этого локуса, а также **модификаторные гены** (T, B), меняющие его функцию.

**Основные аллели локуса M** (по убыванию активности белка):

| Аллель | Функция | Фенотип |
|--------|---------|---------|
| `M` | Полноценный белок | Прямое управление маной |
| `B` | Изменённый белок (биологический уклон) | Магическая биология |
| `T` | Изменённый белок (техномагический уклон) | Магическая механика/химия |
| `N` | Нефункциональный белок (делеция в промоторе) | Слабое или нулевое управление маной |

> Это пример **множественных аллелей** с эпистатическими взаимодействиями.

### 2. Иерархия «силы»

Последовательность от сильного к слабому: **N → T → B → M** — означает **эпистатическое подавление**:
- Аллель `N` (нулевой) рецессивен ко всем остальным — проявляется только в гомозиготе `NN`.
- Аллели `T` и `B` доминантны над `M`, `T` подавляет `B`.

### 3. Запрет на слияние одинаковых магических гамет

> **Единственное магическое допущение.**

Продукт гена M экспрессируется на поверхности половых клеток. При контакте двух гамет с одинаковым аллелем происходит **иммуноподобная реакция отторжения**, блокирующая слияние.

**Запрещены:** `M+M`, `T+T`.  
**Разрешены:** `B+B` (запрета нет), а также любые смешанные пары.

У эльфов есть дополнительный ген-супрессор `S` (эльфийский фактор совместимости), который подавляет реакцию отторжения для пары `M+M`. Благодаря этому у эльфов возможно слияние двух половых клеток с `M`.

### 4. Распределение по расам

| Раса | Группа | Генотип (локус M) | Модификаторы | Примечание |
|------|--------|-------------------|--------------|-------------|
| **Эльфы** | розовые (P) | `MM` + ген `S` | — | Единственные, у кого разрешено `M+M` |
| **Люди** | розовые (P) | `NN`, `MN` | — | `NN` — немаги, `MN` — слабые маги |
| **Гномы** | розовые (P) | `MT` | `T` | T + P → магические механики |
| **Гоблины** | зелёные (G) | `MT` | `T` | T + G → магические химики |
| **Дриады** | зелёные (G) | `BB` | ген `B` в X-хромосоме | Только женщины |
| **Нимфы** | розовые (P) | `BB` | ген `B` в X-хромосоме | Только женщины |
| **Орки** | зелёные (G) | `MB` | одна копия `B` | Мужчины и женщины |
| **Фавны** | розовые (P) | `MB` | одна копия `B` | Мужчины и женщины |

> **Важно:** Модификатор `T` один и тот же по последовательности, но его эффект зависит от фонового генома:  
> - у розовых (P) → магические механики (гномы)  
> - у зелёных (G) → магические химики (гоблины)

> **Аналогично для B:**  
> - на зелёном фоне (G) → дриады (женщины) и орки (мужчины и женщины)  
> - на розовом фоне (P) → нимфы (женщины) и фавны (мужчины и женщины)

### 5. Наследование дриад/нимф и орков/фавнов (X-сцепление)

Ген `B` находится в **X-хромосоме**. Y-хромосома не несёт функциональной копии.

| Генотип | Пол | Фон G (зелёные) | Фон P (розовые) |
|---------|------|----------------|-----------------|
| `XᴮXᴮ` | Женский | **Дриада** | **Нимфа** |
| `XᴮXᵐ` | Женский | **Орк** | **Фавн** |
| `XᴮY` | Мужской | **Орк** | **Фавн** |

**Почему нет мужчин-дриад и мужчин-нимф?**  
Фенотип дриады/нимфы требует двух копий `B` (гомозигота). У самцов только одна X-хромосома, поэтому они принципиально не могут быть `XᴮXᴮ`.

**Почему `XᴮXᵐ` — орк/фавн, а не дриада/нимфа?**  
Для проявления полного фенотипа (дриада/нимфа) нужны две копии `B`. В гетерозиготе `B/M` работает более слабый вариант — орк на зелёном фоне, фавн на розовом.

### 6. Скрещивание рас

Скрещивание рас возможно и не является межвидовой гибридизацией в биологическом смысле — все гуманоидные расы относятся к одному биологическому виду с высоким полиморфизмом.

#### 6.1. Правила для потомства

1. **Фон P/G** берётся **по матери**.
2. **`S` может прийти и от отца-полуэльфа первого поколения.** Но также только с эльфийкой или полуэльфийкой (также первого поколения) и результат будет эльф.

#### 6.2. Матрица по аллелям (компактная)

Сокращения рас: `Эл` — эльф, `Лд` — человек, `Гн` — гном, `Гб` — гоблин, `Др` — дриада, `Ни` — нимфа, `Ор` — орк, `Фв` — фавн.

| Мать \ Отец | Эл | Лд | Гн/Гб | Ор/Фв |
|-------------|----|----|----|----|
| **Эл** | `MM` | `MN` | `MT` | `MB` |
| **Лд** | `MN` | `MN/NN` | `MT/NT/MN` | `MB/NB/MN` |
| **Гн/Гб** | `MT` | `MT/NT/MN` | `MT` | `MT/BT/MB` |
| **Др/Ни** | `MB` | `MB/NB/MN` | `BT/MB` | `BB/MB` |
| **Ор/Фв** | `MB` | `MB/NB/MN` | `MT/BT/MB` | `BB/MB` |

#### 6.3. Глоссарий типовых полукровок

| Условное имя | Типичные родители (мать × отец) | Суть |
|--------------|----------------------------------|------|
| **Полуэльф** | Эл × Лд (и обратные пары) | `MN`; `MM` вне эльфийской линии нежизнеспособен |
| **Полугном** | Человек × гном; эльф × гном; гном × гоблин | Автосомы `MT`/`NT`/`MN`; фон «розовый» → **механика** |
| **Полугоблин** | Человек × гоблин; эльф × гоблин; гном × гоблин | Как полугном, но фон «зелёный» → **химия** |
| **Полуорк** | Орк / дриада × эльф / человек / гном / гоблин | Смесь с носителем `B` и фоном **G** |
| **Полуфавн** | Фавн / нимфа × эльф / человек / гном / гоблин | Смесь  с носителем `B` при фоне **P** |
